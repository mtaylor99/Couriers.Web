import { styled, Theme } from "@material-ui/core";
// eslint-disable-next-line import/prefer-default-export
export const Intro = styled("div")<Theme>(({ theme }) => ({
  marginBottom: theme.spacing(4),
}));
